name: 'sa_qlfs_silver_gold'
version: '1.0.0'
config-version: 2

flags:
  require_explicit_package_overrides_for_builtin_materializations: false

profile: 'sa_qlfs_silver_gold'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  sa_qlfs_silver_gold:
    staging:
      +materialized: view
      +schema: silver
    marts:
      +materialized: table
      +schema: gold
  elementary:
    +schema: elementary
